package main

import (
	"github.com/qiangxue/fasthttp-routing"
)

func handleTwitter(context *routing.Context) error {
	// hash := c.Param("hash")
	// log.Println("Got hash :", hash)

	// return routing.NewHTTPError(400, "Not a post.")

	return nil
}
